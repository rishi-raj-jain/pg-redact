# Neon Postgres connection string (direct, unpooled is fine for the HTTP driver).
DATABASE_URL="postgresql://<user>:<password>@<endpoint>.us-east-2.aws.neon.tech/neondb?sslmode=require"

# TypeSafe / Jev API key from https://typesafe.ai (server-side only).
TYPESAFE_API_KEY="apikey_..."
